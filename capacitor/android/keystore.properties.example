# Copy to keystore.properties on the build machine only.
# Never commit keystore.properties or the keystore file.

storeFile=keystores/gtc-academy-release.p12
storeType=PKCS12
storePassword=CHANGE_ME
keyAlias=gtc-academy-release
keyPassword=CHANGE_ME
